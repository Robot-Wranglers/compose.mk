#!/usr/bin/env -S ./compose.mk cmk run
# structured-io.cmk:
#   Emit and consume JSON with the 🡄 / 🡆 arrow operators.
#   Ported from demos/structured-io.mk.

emit: 
	🡄 key=val

consume:
	🡆 .key

__main__:
	this.emit | this.consume
	cmk.log.target(hello world!)