{% import 'macros/base.j2' as macros -%}{% import 'macros/site.j2' as site -%}

#### API: lang
<hr class="section-rule lvl-4">

The compile / lower / preprocess pipeline itself is documented on the [compiler page]({{_}}/cmk/compiler); this namespace is the surrounding CMK-language tooling: linting, doc-block extraction, size metrics, and source-forking / packaging.

{{site.parse_help('compose.mk', 'lang', github)}}
