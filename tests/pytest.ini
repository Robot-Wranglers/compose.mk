[pytest]
testpaths = .
addopts = -ra
markers =
    unit: pure target I/O, no docker
    integration: scaffolds a project dir, needs a docker daemon
    compiler: CMK->Makefile transpile/golden, no docker
    docker: requires a docker daemon
